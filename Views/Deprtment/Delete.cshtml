@model EmployeesDashboard.Models.DepartmentViewModel

<form asp-action="Delete" asp-controller="Deprtment">
    <div class="row">
        <div class="col-8">
            <partial name="_DetailsDeleteDepartPartialView" model="@Model" />
            <input asp-for="@Model.Id" hidden />
            <div>
                <a class="btn btn-dark" asp-action="Index">Index</a>
                <input type="submit" value="Delete" class="btn btn-danger" asp-route-id="@Model.Id" />

            </div>
        </div>
    </div>
</form>

